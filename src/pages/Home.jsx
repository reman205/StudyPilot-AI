import { useContext } from 'react'; import { useNavigate } from 'react-router-dom'; import { AppContext } from '../App'; import Card from '../components/Card'; import Button from '../components/Button'; import ProgressBar from '../components/ProgressBar'; import ProgressRing from '../components/ProgressRing'; import { ArrowRight,Clock,Flame,Leaf,Calendar,TrendingUp } from 'lucide-react';
export default function Home(){const {student,progress,courses}=useContext(AppContext);const nav=useNavigate();return <div className="stack"><header className="page-title"><div><h1>Good Evening, {student.name} 👋</h1><p>Let’s continue your learning journey.</p></div></header><Card className="mission dark"><div><span className="eyebrow">Today’s Mission</span><h2>Linear Algebra — Kernel</h2><p><Clock size={15}/> 25 minutes</p></div><Button variant="light" icon={ArrowRight} onClick={()=>nav('/courses/linalg/learn')}>Continue Learning</Button></Card><div className="metric-grid"><Card className="metric"><ProgressRing value={82}/><div><span className="eyebrow">Exam Readiness</span><strong>On track</strong><small>You’re building consistent progress.</small></div></Card><Card className="metric"><span className="metric-icon gold"><Flame/></span><div><span className="eyebrow">Daily Streak</span><strong>{progress.streak} days</strong><small>Keep your calm momentum.</small></div></Card><Card className="metric"><span className="metric-icon sage"><Leaf/></span><div><span className="eyebrow">Study Garden</span><strong>Growing</strong><small>Your garden grew today.</small></div></Card></div><section><div className="section-head"><h2>Active Courses</h2><button onClick={()=>nav('/courses')}>See all →</button></div><div className="course-list">{courses.map(c=><Card key={c.id} className="course-row" onClick={()=>nav(`/courses/${c.id}`)}><span className="course-icon"><c.icon/></span><div><strong>{c.name}</strong><small>Next: {c.nextLesson}</small><ProgressBar value={c.progress}/></div><b>{c.progress}%</b></Card>)}</div></section><div className="two-col"><Card><span className="eyebrow"><Calendar size={15}/> Upcoming Exam</span><h3>Linear Algebra</h3><p>5 days remaining</p></Card><Card><span className="eyebrow"><TrendingUp size={15}/> Weekly Study Summary</span><h3>6h 40m studied</h3><p>12 concepts reviewed • 3 quizzes completed</p></Card></div></div>}
