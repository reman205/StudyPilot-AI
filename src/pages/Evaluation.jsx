import Card from '../components/Card'; import ProgressRing from '../components/ProgressRing'; import Badge from '../components/Badge'; import { goldenCases } from '../data/agentData'; import { CheckCircle2, Gauge, RefreshCw } from 'lucide-react'; import Button from '../components/Button';
export default function Evaluation(){return <div className="stack"><header className="page-title"><div><span className="eyebrow">Day 6 • Evaluation</span><h1>System Evaluation</h1><p>Repeatable tests measure outcomes, process quality and regression risk.</p></div><Button variant="secondary" icon={RefreshCw}>Run Evaluation</Button></header><div className="metric-grid"><Card className="metric"><ProgressRing value={97}/><div><span className="eyebrow">Golden Dataset</span><strong>97% pass score</strong><small>5 reference cases</small></div></Card><Card className="metric"><span className="metric-icon sage"><Gauge/></span><div><span className="eyebrow">Process Compliance</span><strong>100%</strong><small>Correct tools and routing</small></div></Card><Card className="metric"><span className="metric-icon gold"><CheckCircle2/></span><div><span className="eyebrow">Regression Status</span><strong>None detected</strong><small>Compared with last run</small></div></Card></div><Card><div className="section-head"><div><span className="eyebrow">Golden Dataset</span><h2>Reference test cases</h2></div><Badge tone="success">Last run passed</Badge></div><div className="evaluation-list">{goldenCases.map(c=><div className="evaluation-row" key={c.id}><span className="case-id">{c.id}</span><div><strong>{c.input}</strong><small>Expected: {c.expected}</small></div><b>{c.score}%</b><Badge tone="success">{c.status}</Badge></div>)}</div></Card><div className="two-col"><Card><h3>Evaluation dimensions</h3><div className="check-row"><CheckCircle2 size={16}/> Outcome correctness</div><div className="check-row"><CheckCircle2 size={16}/> Process and tool compliance</div><div className="check-row"><CheckCircle2 size={16}/> Human review for sensitive cases</div></Card><Card><h3>Quality gate</h3><p>Deployment is blocked if the golden dataset falls below 90%, a security case fails, or an agent uses an unauthorized tool.</p><Badge tone="success">Gate open</Badge></Card></div></div>}
