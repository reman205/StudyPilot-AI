import Card from '../components/Card'; import Badge from '../components/Badge'; import { initialLogs } from '../data/agentData'; import { ScrollText, Filter } from 'lucide-react'; import Button from '../components/Button';
export default function SystemLogs(){return <div className="stack"><header className="page-title"><div><span className="eyebrow">Monitoring & Observability</span><h1>System Logs</h1><p>Trace agent routing, tool usage, validation and final delivery.</p></div><Button variant="secondary" icon={Filter}>Filter</Button></header><div className="metric-grid"><Card className="metric"><span className="metric-icon blue"><ScrollText/></span><div><span className="eyebrow">Events Today</span><strong>42</strong><small>Across 7 agents</small></div></Card><Card className="metric"><div><span className="eyebrow">Average Workflow</span><strong>5.2 sec</strong><small>Within target</small></div></Card><Card className="metric"><div><span className="eyebrow">Failed Actions</span><strong>0</strong><small>No intervention required</small></div></Card></div><Card><div className="section-head"><h2>Latest execution trace</h2><Badge tone="success">Healthy</Badge></div><div className="log-list">{initialLogs.map((l,i)=><div className="log-row" key={i}><span className={`log-dot ${l.tone}`}/><time>{l.time}</time><b>{l.agent}</b><div><strong>{l.event}</strong><small>{l.detail}</small></div></div>)}</div></Card><Card><h3>Why logs matter</h3><p>Structured events make it possible to diagnose failures, measure agent performance, verify execution order and understand why a result was produced.</p></Card></div>}
