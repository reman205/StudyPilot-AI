import Card from '../components/Card'; import Badge from '../components/Badge'; import { skills, tools } from '../data/agentData'; import { Puzzle, Wrench } from 'lucide-react';
export default function SkillsLibrary(){return <div className="stack"><header className="page-title"><div><span className="eyebrow">Reusable Capabilities</span><h1>Skills & Tools</h1><p>Specialized instructions teach each agent how to perform a repeatable academic task.</p></div></header><div className="metric-grid"><Card className="metric"><span className="metric-icon blue"><Puzzle/></span><div><span className="eyebrow">Active Skills</span><strong>{skills.length}</strong><small>Reusable across workflows</small></div></Card><Card className="metric"><span className="metric-icon sage"><Wrench/></span><div><span className="eyebrow">Registered Tools</span><strong>{tools.length}</strong><small>Controlled by permissions</small></div></Card></div><section><div className="section-head"><h2>Skills Library</h2><Badge tone="success">All operational</Badge></div><div className="skills-grid">{skills.map(s=><Card key={s.name} className="skill-card"><div className="skill-top"><div className="agent-icon"><Puzzle size={20}/></div><Badge tone="success">{s.status}</Badge></div><h3>{s.name}</h3><p>{s.description}</p><div className="skill-meta"><span>{s.category}</span><span>Used by {s.usedBy}</span></div></Card>)}</div></section><section><div className="section-head"><h2>Local Tool Registry</h2><span className="soft-label">MCP-ready architecture</span></div><Card className="table-card"><div className="data-table"><div className="table-head"><span>Tool</span><span>Access</span><span>Agents</span></div>{tools.map(t=><div className="table-row" key={t.name}><span className="tool-name"><t.icon size={17}/><b>{t.name}</b></span><span>{t.access}</span><span>{t.agents}</span></div>)}</div></Card></section></div>}
