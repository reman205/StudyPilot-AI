import Card from '../components/Card'; import Badge from '../components/Badge'; import { ShieldCheck, LockKeyhole, UserCheck, ScanSearch, AlertTriangle, CheckCircle2 } from 'lucide-react';
const controls=[['Prompt Injection Guard','External content is treated as data, never as system instructions.',ScanSearch],['Least Privilege','Each agent receives only the tools required for its role.',LockKeyhole],['Human Approval Gates','Reset, deletion and future external actions require confirmation.',UserCheck],['Output Validation','Nova verifies structure, safety and source labels before delivery.',ShieldCheck]];
export default function SecurityCenter(){return <div className="stack"><header className="page-title"><div><span className="eyebrow">Day 6 • Security</span><h1>Security Center</h1><p>Controls protect student data and constrain every agent’s actions.</p></div><Badge tone="success">Protected</Badge></header><Card className="security-banner"><ShieldCheck size={30}/><div><h2>All core safeguards are active</h2><p>This prototype demonstrates security policy, permissions and validation using local mock workflows.</p></div></Card><div className="security-grid">{controls.map(([name,desc,Icon])=><Card key={name} className="security-control"><div className="skill-top"><span className="metric-icon sage"><Icon/></span><Badge tone="success">Enabled</Badge></div><h3>{name}</h3><p>{desc}</p><div className="check-row"><CheckCircle2 size={16}/> Last check passed</div></Card>)}</div><Card><div className="section-head"><h2>Permission matrix</h2><span className="soft-label">Least privilege</span></div><div className="permission-grid"><div><b>Course files</b><span>Read: Atlas, Clarity, Prism, Finale</span><span>Write: none</span></div><div><b>Study plan</b><span>Read: Nova, Tempo</span><span>Write: Tempo only</span></div><div><b>Quiz results</b><span>Read: Nova, Prism, Pulse</span><span>Write: Prism only</span></div><div><b>Profile settings</b><span>Read: Nova</span><span>Write: student only</span></div></div></Card><Card className="warning-card"><AlertTriangle size={20}/><div><strong>Human gate example</strong><p>Resetting demo data still requires direct user confirmation from the Profile page.</p></div></Card></div>}
